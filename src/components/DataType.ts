export interface Data {
  id: number;
  name: string;
  price: number;
  img: string;
  inStock: number;
  fastDelivery: boolean;
  rating: number;
  desc: string;
}

export interface Currency {
  label: string;
  symbol: string;
}
